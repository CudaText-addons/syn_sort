Plugin for SynWrite.
Gives commands/dialog to sort lines.

To allow to sort all text, if no selection, set the option:
create config file by command "Edit config", and write option:
[op]
sort_all=1


Author: Alexey T
